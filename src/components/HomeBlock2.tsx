import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div class="section-v-pad bg-dark">
      <div class="reveal-up wrapper grid division-gap text-white text-center">
        <div class="text-sm font-bold">DOING THINGS DIFFERENTLY</div>
        <h2>
          Global music distribution, promotion & rights management for
          independent artists & labels looking for more.
        </h2>
      </div>
    </div>
  );
});
